VITE_API_URL=http://localhost:5000
# In production on Railway, leave empty or use the same origin
# The frontend will use relative paths (/api, /datasets) automatically
# VITE_API_URL can be left empty in production since frontend is served from same domain
